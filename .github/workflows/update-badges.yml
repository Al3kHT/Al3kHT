name: Update Badges
on:
  schedule:
    - cron: "0 2 * * *"  # Diariamente a las 2 AM UTC
  workflow_dispatch:  # Permite ejecución manual
jobs:
  update-badges:
    name: Update README with Credly Badges
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Update Badges
        uses: pemtajo/badge-readme@main
        with:
          CREDLY_USER: mata.nui5.akht@gmail.com  # Ej: tu email o username en Credly
          NUMBER_LAST_BADGES: 5  # Muestra los últimos 5 badges (o 0 para todos)
          COMMIT_MESSAGE: "🔄 Updated Credly badges"
